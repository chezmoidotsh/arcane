apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-custom-namespace
  labels:
    app.kubernetes.io/name: app-custom-namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: app-custom-namespace
  template:
    metadata:
      labels:
        app.kubernetes.io/name: app-custom-namespace
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 80
