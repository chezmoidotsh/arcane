apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-manual-sync
  labels:
    app.kubernetes.io/name: app-manual-sync
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: app-manual-sync
  template:
    metadata:
      labels:
        app.kubernetes.io/name: app-manual-sync
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 80
