apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-default
  labels:
    app.kubernetes.io/name: app-default
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: app-default
  template:
    metadata:
      labels:
        app.kubernetes.io/name: app-default
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
          ports:
            - containerPort: 80
