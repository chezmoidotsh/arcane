{{- /*
cluster-networkpolicies.yaml
Renders per-cluster CiliumNetworkPolicy resources for application-level access.

Note: operator ingress (5432, 8000) and kube-apiserver egress are intentionally
NOT managed here. They are covered by CiliumClusterwideNetworkPolicy resources
shipped with the CNPG operator itself:
  catalog/kubernetes/cloudnative-pg/kustomize/cnpg.cluster-policy.allow-from-operator.yaml
  catalog/kubernetes/cloudnative-pg/kustomize/cnpg.cluster-policy.allow-to-kubernetes-api.yaml
*/ -}}

{{- if .Values.spec.behavior.networkPolicies.enabled -}}
{{- end -}}
