{
    "name": "@catalog.chezmoi.sh/os~alpine-3.19",
    "version": "3.19.2",
    "private": true,
    "description": "Alpine Linux is a security-oriented, lightweight Linux distribution based on musl libc and busybox.",
    "license": "Apache-2.0",
    "author": "Alexandre Nicolaie <xunleii@users.noreply.github.com>",
    "scripts": {
        "lint": "trunk fmt .",
        "test": "vitest run .",
        "test:coverage": "vitest run --coverage .",
        "test:dev": "vitest run --coverage --ui ."
    },
    "dependencies": {
        "@catalog.chezmoi.sh/os~alpine": "workspace:*",
        "@pulumi/docker-build": "~0.0.4",
        "@pulumi/pulumi": "^3.120.0"
    },
    "devDependencies": {
        "@pulumi.chezmoi.sh/vitest-scenario": "workspace:*"
    }
}
