// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * The provider type for the truenas package. By default, resources use package-wide configuration
 * settings, however an explicit `Provider` instance may be created and passed during resource
 * construction to achieve fine-grained programmatic control over provider settings. See the
 * [documentation](https://www.pulumi.com/docs/reference/programming-model/#providers) for more information.
 */
export class Provider extends pulumi.ProviderResource {
	/** @internal */
	public static readonly __pulumiType = "truenas";

	/**
	 * Returns true if the given object is an instance of Provider.  This is designed to work even
	 * when multiple copies of the Pulumi SDK have been loaded into the same process.
	 */
	public static isInstance(obj: any): obj is Provider {
		if (obj === undefined || obj === null) {
			return false;
		}
		return obj["__pulumiType"] === "pulumi:providers:" + Provider.__pulumiType;
	}

	/**
	 * The API key for authenticating with TrueNAS SCALE. Can also be set via the TRUENAS_API_KEY environment variable.
	 */
	public declare readonly apiKey: pulumi.Output<string | undefined>;
	/**
	 * Per-request HTTP timeout, parsed via time.ParseDuration (e.g. "60s", "2m", "5m"). Raise this for loaded production TrueNAS instances where pool scrub status, large dataset listings, or replication checks may take longer than the default 60s. Lower it for local/CI testing. Can also be set via the TRUENAS_REQUEST_TIMEOUT environment variable. Values of zero or less are ignored so this attribute can never disable the timeout.
	 */
	public declare readonly requestTimeout: pulumi.Output<string | undefined>;
	/**
	 * The base URL of the TrueNAS SCALE instance (e.g., https://truenas.example.com). Can also be set via the TRUENAS_URL environment variable.
	 */
	public declare readonly url: pulumi.Output<string | undefined>;

	/**
	 * Create a Provider resource with the given unique name, arguments, and options.
	 *
	 * @param name The _unique_ name of the resource.
	 * @param args The arguments to use to populate this resource's properties.
	 * @param opts A bag of options that control this resource's behavior.
	 */
	constructor(
		name: string,
		args?: ProviderArgs,
		opts?: pulumi.ResourceOptions,
	) {
		const resourceInputs: pulumi.Inputs = {};
		opts = opts || {};
		resourceInputs["apiKey"] = args?.apiKey
			? pulumi.secret(args.apiKey)
			: undefined;
		resourceInputs["destroyProtection"] = pulumi
			.output(args?.destroyProtection)
			.apply(JSON.stringify);
		resourceInputs["insecureSkipVerify"] = pulumi
			.output(args?.insecureSkipVerify)
			.apply(JSON.stringify);
		resourceInputs["readOnly"] = pulumi
			.output(args?.readOnly)
			.apply(JSON.stringify);
		resourceInputs["requestTimeout"] = args?.requestTimeout;
		resourceInputs["url"] = args?.url;
		opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
		const secretOpts = { additionalSecretOutputs: ["apiKey"] };
		opts = pulumi.mergeOptions(opts, secretOpts);
		super(
			Provider.__pulumiType,
			name,
			resourceInputs,
			opts,
			false /*dependency*/,
			utilities.getPackage(),
		);
	}

	/**
	 * This function returns a Terraform config object with terraform-namecased keys,to be used with the Terraform Module Provider.
	 */
	terraformConfig(): pulumi.Output<{ [key: string]: any }> {
		const result: pulumi.Output<Provider.TerraformConfigResult> =
			pulumi.runtime.call(
				"pulumi:providers:truenas/terraformConfig",
				{
					__self__: this,
				},
				this,
				utilities.getPackage(),
			);
		return result.result;
	}
}

/**
 * The set of arguments for constructing a Provider resource.
 */
export interface ProviderArgs {
	/**
	 * The API key for authenticating with TrueNAS SCALE. Can also be set via the TRUENAS_API_KEY environment variable.
	 */
	apiKey?: pulumi.Input<string | undefined>;
	/**
	 * When true, the provider refuses DELETE requests at the client layer while still allowing GET/POST/PUT through. This is the apply-safe rail for the first production mutations: creates and updates work normally but no resource can be destroyed until the flag is cleared. Equivalent to per-resource <span pulumi-lang-nodejs="`deletionProtection`" pulumi-lang-dotnet="`DeletionProtection`" pulumi-lang-go="`deletionProtection`" pulumi-lang-python="`deletion_protection`" pulumi-lang-yaml="`deletionProtection`" pulumi-lang-java="`deletionProtection`" pulumi-lang-hcl="`deletion_protection`">`deletionProtection`</span> flags found in major Terraform providers, but enforced at the wire for every resource in the provider at once. Layer it with `read_only=true` for Phase 0 (read-only plan against prod) and drop <span pulumi-lang-nodejs="`readOnly`" pulumi-lang-dotnet="`ReadOnly`" pulumi-lang-go="`readOnly`" pulumi-lang-python="`read_only`" pulumi-lang-yaml="`readOnly`" pulumi-lang-java="`readOnly`" pulumi-lang-hcl="`read_only`">`readOnly`</span> + keep `destroy_protection=true` for Phase 1 (safe-apply: create and update only). Can also be set via the TRUENAS_DESTROY_PROTECTION environment variable. Defaults to false for backwards compatibility; the docs/guides/phased-rollout.md guide strongly recommends setting this to true for any HCL pointed at production.
	 */
	destroyProtection?: pulumi.Input<boolean | undefined>;
	/**
	 * Skip TLS certificate verification. Only use this for self-signed test environments. Can also be set via the TRUENAS_INSECURE_SKIP_VERIFY environment variable.
	 */
	insecureSkipVerify?: pulumi.Input<boolean | undefined>;
	/**
	 * When true, the provider refuses every mutating request (POST/PUT/DELETE) before it reaches the network. Intended for phased production rollout, point the provider at a real TrueNAS instance, run `terraform plan`, and be physically unable to mutate anything. Any resource that would be changed surfaces as a normal Terraform error rather than a partial write. Can also be set via the TRUENAS_READONLY environment variable. Defaults to false.
	 */
	readOnly?: pulumi.Input<boolean | undefined>;
	/**
	 * Per-request HTTP timeout, parsed via time.ParseDuration (e.g. "60s", "2m", "5m"). Raise this for loaded production TrueNAS instances where pool scrub status, large dataset listings, or replication checks may take longer than the default 60s. Lower it for local/CI testing. Can also be set via the TRUENAS_REQUEST_TIMEOUT environment variable. Values of zero or less are ignored so this attribute can never disable the timeout.
	 */
	requestTimeout?: pulumi.Input<string | undefined>;
	/**
	 * The base URL of the TrueNAS SCALE instance (e.g., https://truenas.example.com). Can also be set via the TRUENAS_URL environment variable.
	 */
	url?: pulumi.Input<string | undefined>;
}

export namespace Provider {
	/**
	 * The results of the Provider.terraformConfig method.
	 */
	export interface TerraformConfigResult {
		readonly result: { [key: string]: any };
	}
}
