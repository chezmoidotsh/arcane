// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import type * as inputs from "./types/input";
import type * as outputs from "./types/output";
import * as utilities from "./utilities";

export class VmDevice extends pulumi.CustomResource {
	/**
	 * Get an existing VmDevice resource's state with the given name, ID, and optional extra
	 * properties used to qualify the lookup.
	 *
	 * @param name The _unique_ name of the resulting resource.
	 * @param id The _unique_ provider ID of the resource to lookup.
	 * @param state Any extra arguments used during the lookup.
	 * @param opts Optional settings to control the behavior of the CustomResource.
	 */
	public static get(
		name: string,
		id: pulumi.Input<pulumi.ID>,
		state?: VmDeviceState,
		opts?: pulumi.CustomResourceOptions,
	): VmDevice {
		return new VmDevice(name, <any>state, { ...opts, id: id });
	}

	/** @internal */
	public static readonly __pulumiType = "truenas:index/vmDevice:VmDevice";

	/**
	 * Returns true if the given object is an instance of VmDevice.  This is designed to work even
	 * when multiple copies of the Pulumi SDK have been loaded into the same process.
	 */
	public static isInstance(obj: any): obj is VmDevice {
		if (obj === undefined || obj === null) {
			return false;
		}
		return obj["__pulumiType"] === VmDevice.__pulumiType;
	}

	/**
	 * Type-specific device attributes as string values. For example DISK uses path, type (AHCI/VIRTIO), iotype; NIC uses type, mac, nic_attach; DISPLAY uses resolution, bind, port, password, web; CDROM uses path; RAW uses path, type, size; PCI uses pptdev; USB uses controller_type, usb. DISPLAY devices may contain a VNC/SPICE password, marked sensitive.
	 */
	public declare readonly attributes: pulumi.Output<{ [key: string]: string }>;
	/**
	 * The device type: DISK, NIC, CDROM, DISPLAY, RAW, PCI, or USB. Changing this forces replacement.
	 */
	public declare readonly dtype: pulumi.Output<string>;
	/**
	 * Device order on the VM's bus. Optional.
	 */
	public declare readonly order: pulumi.Output<number>;
	public declare readonly timeouts: pulumi.Output<
		outputs.VmDeviceTimeouts | undefined
	>;
	/**
	 * The ID of the VM this device is attached to.
	 */
	public declare readonly vm: pulumi.Output<number>;

	/**
	 * Create a VmDevice resource with the given unique name, arguments, and options.
	 *
	 * @param name The _unique_ name of the resource.
	 * @param args The arguments to use to populate this resource's properties.
	 * @param opts A bag of options that control this resource's behavior.
	 */
	constructor(
		name: string,
		args: VmDeviceArgs,
		opts?: pulumi.CustomResourceOptions,
	);
	constructor(
		name: string,
		argsOrState?: VmDeviceArgs | VmDeviceState,
		opts?: pulumi.CustomResourceOptions,
	) {
		const resourceInputs: pulumi.Inputs = {};
		opts = opts || {};
		if (opts.id) {
			const state = argsOrState as VmDeviceState | undefined;
			resourceInputs["attributes"] = state?.attributes;
			resourceInputs["dtype"] = state?.dtype;
			resourceInputs["order"] = state?.order;
			resourceInputs["timeouts"] = state?.timeouts;
			resourceInputs["vm"] = state?.vm;
		} else {
			const args = argsOrState as VmDeviceArgs | undefined;
			if (args?.attributes === undefined && !opts.urn) {
				throw new Error("Missing required property 'attributes'");
			}
			if (args?.dtype === undefined && !opts.urn) {
				throw new Error("Missing required property 'dtype'");
			}
			if (args?.vm === undefined && !opts.urn) {
				throw new Error("Missing required property 'vm'");
			}
			resourceInputs["attributes"] = args?.attributes
				? pulumi.secret(args.attributes)
				: undefined;
			resourceInputs["dtype"] = args?.dtype;
			resourceInputs["order"] = args?.order;
			resourceInputs["timeouts"] = args?.timeouts;
			resourceInputs["vm"] = args?.vm;
		}
		opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
		const secretOpts = { additionalSecretOutputs: ["attributes"] };
		opts = pulumi.mergeOptions(opts, secretOpts);
		super(
			VmDevice.__pulumiType,
			name,
			resourceInputs,
			opts,
			false /*dependency*/,
			utilities.getPackage(),
		);
	}
}

/**
 * Input properties used for looking up and filtering VmDevice resources.
 */
export interface VmDeviceState {
	/**
	 * Type-specific device attributes as string values. For example DISK uses path, type (AHCI/VIRTIO), iotype; NIC uses type, mac, nic_attach; DISPLAY uses resolution, bind, port, password, web; CDROM uses path; RAW uses path, type, size; PCI uses pptdev; USB uses controller_type, usb. DISPLAY devices may contain a VNC/SPICE password, marked sensitive.
	 */
	attributes?: pulumi.Input<
		{ [key: string]: pulumi.Input<string> } | undefined
	>;
	/**
	 * The device type: DISK, NIC, CDROM, DISPLAY, RAW, PCI, or USB. Changing this forces replacement.
	 */
	dtype?: pulumi.Input<string | undefined>;
	/**
	 * Device order on the VM's bus. Optional.
	 */
	order?: pulumi.Input<number | undefined>;
	timeouts?: pulumi.Input<inputs.VmDeviceTimeouts | undefined>;
	/**
	 * The ID of the VM this device is attached to.
	 */
	vm?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a VmDevice resource.
 */
export interface VmDeviceArgs {
	/**
	 * Type-specific device attributes as string values. For example DISK uses path, type (AHCI/VIRTIO), iotype; NIC uses type, mac, nic_attach; DISPLAY uses resolution, bind, port, password, web; CDROM uses path; RAW uses path, type, size; PCI uses pptdev; USB uses controller_type, usb. DISPLAY devices may contain a VNC/SPICE password, marked sensitive.
	 */
	attributes: pulumi.Input<{ [key: string]: pulumi.Input<string> }>;
	/**
	 * The device type: DISK, NIC, CDROM, DISPLAY, RAW, PCI, or USB. Changing this forces replacement.
	 */
	dtype: pulumi.Input<string>;
	/**
	 * Device order on the VM's bus. Optional.
	 */
	order?: pulumi.Input<number | undefined>;
	timeouts?: pulumi.Input<inputs.VmDeviceTimeouts | undefined>;
	/**
	 * The ID of the VM this device is attached to.
	 */
	vm: pulumi.Input<number>;
}
