// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import type * as inputs from "./types/input";
import type * as outputs from "./types/output";
import * as utilities from "./utilities";

export class Directory extends pulumi.CustomResource {
	/**
	 * Get an existing Directory resource's state with the given name, ID, and optional extra
	 * properties used to qualify the lookup.
	 *
	 * @param name The _unique_ name of the resulting resource.
	 * @param id The _unique_ provider ID of the resource to lookup.
	 * @param state Any extra arguments used during the lookup.
	 * @param opts Optional settings to control the behavior of the CustomResource.
	 */
	public static get(
		name: string,
		id: pulumi.Input<pulumi.ID>,
		state?: DirectoryState,
		opts?: pulumi.CustomResourceOptions,
	): Directory {
		return new Directory(name, <any>state, { ...opts, id: id });
	}

	/** @internal */
	public static readonly __pulumiType = "truenas:index/directory:Directory";

	/**
	 * Returns true if the given object is an instance of Directory.  This is designed to work even
	 * when multiple copies of the Pulumi SDK have been loaded into the same process.
	 */
	public static isInstance(obj: any): obj is Directory {
		if (obj === undefined || obj === null) {
			return false;
		}
		return obj["__pulumiType"] === Directory.__pulumiType;
	}

	/**
	 * When true, create any missing parent directories before the leaf (like mkdir -p).
	 */
	public declare readonly createParents: pulumi.Output<boolean>;
	/**
	 * The owner GID. Applied via setperm when set or changed.
	 */
	public declare readonly gid: pulumi.Output<number>;
	/**
	 * The octal permission mode for the directory (e.g., 755). Permission bits only: the TrueNAS filesystem API rejects modes above 777, so setuid/setgid/sticky bits cannot be set through it.
	 */
	public declare readonly mode: pulumi.Output<string>;
	/**
	 * The absolute filesystem path of the directory (e.g., /mnt/tank/media). Must be under /mnt/.
	 */
	public declare readonly path: pulumi.Output<string>;
	public declare readonly timeouts: pulumi.Output<
		outputs.DirectoryTimeouts | undefined
	>;
	/**
	 * The owner UID. Applied via setperm when set or changed.
	 */
	public declare readonly uid: pulumi.Output<number>;

	/**
	 * Create a Directory resource with the given unique name, arguments, and options.
	 *
	 * @param name The _unique_ name of the resource.
	 * @param args The arguments to use to populate this resource's properties.
	 * @param opts A bag of options that control this resource's behavior.
	 */
	constructor(
		name: string,
		args: DirectoryArgs,
		opts?: pulumi.CustomResourceOptions,
	);
	constructor(
		name: string,
		argsOrState?: DirectoryArgs | DirectoryState,
		opts?: pulumi.CustomResourceOptions,
	) {
		const resourceInputs: pulumi.Inputs = {};
		opts = opts || {};
		if (opts.id) {
			const state = argsOrState as DirectoryState | undefined;
			resourceInputs["createParents"] = state?.createParents;
			resourceInputs["gid"] = state?.gid;
			resourceInputs["mode"] = state?.mode;
			resourceInputs["path"] = state?.path;
			resourceInputs["timeouts"] = state?.timeouts;
			resourceInputs["uid"] = state?.uid;
		} else {
			const args = argsOrState as DirectoryArgs | undefined;
			if (args?.path === undefined && !opts.urn) {
				throw new Error("Missing required property 'path'");
			}
			resourceInputs["createParents"] = args?.createParents;
			resourceInputs["gid"] = args?.gid;
			resourceInputs["mode"] = args?.mode;
			resourceInputs["path"] = args?.path;
			resourceInputs["timeouts"] = args?.timeouts;
			resourceInputs["uid"] = args?.uid;
		}
		opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
		super(
			Directory.__pulumiType,
			name,
			resourceInputs,
			opts,
			false /*dependency*/,
			utilities.getPackage(),
		);
	}
}

/**
 * Input properties used for looking up and filtering Directory resources.
 */
export interface DirectoryState {
	/**
	 * When true, create any missing parent directories before the leaf (like mkdir -p).
	 */
	createParents?: pulumi.Input<boolean | undefined>;
	/**
	 * The owner GID. Applied via setperm when set or changed.
	 */
	gid?: pulumi.Input<number | undefined>;
	/**
	 * The octal permission mode for the directory (e.g., 755). Permission bits only: the TrueNAS filesystem API rejects modes above 777, so setuid/setgid/sticky bits cannot be set through it.
	 */
	mode?: pulumi.Input<string | undefined>;
	/**
	 * The absolute filesystem path of the directory (e.g., /mnt/tank/media). Must be under /mnt/.
	 */
	path?: pulumi.Input<string | undefined>;
	timeouts?: pulumi.Input<inputs.DirectoryTimeouts | undefined>;
	/**
	 * The owner UID. Applied via setperm when set or changed.
	 */
	uid?: pulumi.Input<number | undefined>;
}

/**
 * The set of arguments for constructing a Directory resource.
 */
export interface DirectoryArgs {
	/**
	 * When true, create any missing parent directories before the leaf (like mkdir -p).
	 */
	createParents?: pulumi.Input<boolean | undefined>;
	/**
	 * The owner GID. Applied via setperm when set or changed.
	 */
	gid?: pulumi.Input<number | undefined>;
	/**
	 * The octal permission mode for the directory (e.g., 755). Permission bits only: the TrueNAS filesystem API rejects modes above 777, so setuid/setgid/sticky bits cannot be set through it.
	 */
	mode?: pulumi.Input<string | undefined>;
	/**
	 * The absolute filesystem path of the directory (e.g., /mnt/tank/media). Must be under /mnt/.
	 */
	path: pulumi.Input<string>;
	timeouts?: pulumi.Input<inputs.DirectoryTimeouts | undefined>;
	/**
	 * The owner UID. Applied via setperm when set or changed.
	 */
	uid?: pulumi.Input<number | undefined>;
}
