{
	"name": "@chezmoi.sh/pulumi-cluster-vault",
	"version": "0.1.0",
	"private": true,
	"description": "Pulumi ComponentResource provisioning OpenBao/Vault access for a Kubernetes cluster (Local/Remote/Tailscaled variants)",
	"main": "src/index.ts",
	"scripts": {
		"test": "mocha",
		"test:watch": "mocha --watch",
		"typecheck:test": "tsc --noEmit -p tsconfig.test.json"
	},
	"dependencies": {
		"@pulumi/pulumi": "^3.250.0",
		"@pulumi/vault": "^7.10.0"
	},
	"devDependencies": {
		"@types/chai": "^4.3.0",
		"@types/mocha": "^10.0.0",
		"@types/node": "^22.0.0",
		"chai": "^4.4.0",
		"mocha": "^10.0.0",
		"ts-node": "^10.9.0",
		"typescript": "^5.6.0"
	}
}
