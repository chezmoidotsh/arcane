## Datastore{{#unless (isSingular datastores)}}s{{/unless}}

{{#each datastores}}
### `{{name}}`{{#if disabled}} _(disabled)_{{/if}}

{{#if comment}}{{{comment}}}{{/if}}

- **Backend**: S3 (Backblaze B2), bucket `{{s3Bucket}}`, endpoint config `{{s3Client}}`
- **Local cache path**: `{{path}}` — chunk cache only, not the full backup set (see `stack/pbs/README.md`, "Datastore architecture")
- **Garbage collection**: `{{gcSchedule}}`
- **Notification delivery**: {{notificationMode}}

{{#if namespaces.length}}
**Namespaces** — a namespace is a path prefix within this datastore (`{{name}}:<namespace>`), not a separate storage
location; each one below can be browsed, restored, and (once scoped that way) pruned/verified independently:

{{#each namespaces}}
- `{{namespace}}`{{#if comment}} — {{{comment}}}{{/if}}
{{/each}}

{{/if}}
{{/each}}
