## Datastore{{#unless (isSingular datastores)}}s{{/unless}}

{{#each datastores}}
### `{{name}}`{{#if disabled}} _(disabled)_{{/if}}

{{#if comment}}{{{comment}}}{{/if}}

- **Backend**: S3 (Backblaze B2), bucket `{{s3Bucket}}`, endpoint config `{{s3Client}}`
- **Local cache path**: `{{path}}` — chunk cache only, not the full backup set (see `stack/proxmox-backup-server/README.md`, "Datastore architecture")
- **Garbage collection**: `{{gcSchedule}}`
- **Notification delivery**: `{{notificationMode}}` — the datastore's notification _mode_, not a target; where events
  actually go is decided by the notification targets and matchers below

{{/each}}
