---
# =============================================================================
# Role: litellm — Handlers
# =============================================================================

- name: Restart LiteLLM
  ansible.builtin.command: launchctl kickstart -k gui/{{ lookup('pipe', 'id -u') }}/sh.chezmoi.shodan.litellm
