---
# =============================================================================
# Role: litellm — Handlers
# =============================================================================

- name: Restart LiteLLM
  ansible.builtin.command: launchctl kickstart -k gui/{{ ansible_user_uid }}/sh.chezmoi.shodan.litellm
