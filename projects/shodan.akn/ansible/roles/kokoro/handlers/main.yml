---
# =============================================================================
# Role: kokoro — Handlers
# =============================================================================

- name: Restart Kokoro
  ansible.builtin.command: launchctl kickstart -k gui/{{ ansible_user_uid }}/sh.chezmoi.shodan.kokoro
