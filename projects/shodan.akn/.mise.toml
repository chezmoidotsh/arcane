# =============================================================================
# Shodan.akn Cluster - Mise Configuration
# =============================================================================
# Project-specific mise configuration for the shodan.akn cluster.
# This cluster configuration follows the Steel Age (A3) architecture
# with Talos Linux and ArgoCD for GitOps deployment.
#
# Services: [Cluster-specific services to be defined]
# Architecture: Talos Linux + ArgoCD
# =============================================================================

[tasks."diagram:generate"]
description = "Generate the architecture diagram for shodan.akn cluster"
# Converts the D2 diagram source to SVG format for documentation
# Input: architecture.d2 (diagram-as-code source)
# Output: assets/architecture.svg (embedded in cluster documentation)
run = "d2 architecture.d2 assets/architecture.svg"
dir = "{{ config_root }}"
