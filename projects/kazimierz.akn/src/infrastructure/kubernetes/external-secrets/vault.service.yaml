apiVersion: v1
kind: Service
metadata:
  annotations:
    tailscale.com/proxy-class: vault
    tailscale.com/tailnet-fqdn: vault.tail831c5d.ts.net
  name: vault-chezmoi-sh
spec:
  externalName: ts.net
  ports:
    - name: https
      port: 443
      protocol: TCP
      targetPort: 443
  sessionAffinity: None
  type: ExternalName
