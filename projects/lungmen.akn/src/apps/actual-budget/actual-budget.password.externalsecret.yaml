apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: actual-budget-admin-password
spec:
  secretStoreRef:
    name: vault.chezmoi.sh
    kind: ClusterSecretStore
  target:
    name: actual-budget-admin-password
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: lungmen.akn/actual-budget/auth/admin-password
