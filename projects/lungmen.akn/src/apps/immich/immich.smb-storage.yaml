---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-smb-storage
  namespace: immich
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: smb
  volumeName: immich-smb-storage
---
apiVersion: v1
kind: PersistentVolume
metadata:
  annotations:
    pv.kubernetes.io/provisioned-by: smb.csi.k8s.io
  name: immich-smb-storage
spec:
  capacity:
    storage: 50Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: smb
  mountOptions:
    - dir_mode=0777
    - file_mode=0777
    - uid=31847
    - gid=31847
    - noperm
    - mfsymlinks
    - cache=strict
    - noserverino # required to prevent data corruption
  csi:
    driver: smb.csi.k8s.io
    # volumeHandle format: {smb-server-address}#{sub-dir-name}#{share-name}
    # make sure this value is unique for every share in the cluster
    volumeHandle: smb-server.default.svc.cluster.local/immich##
    volumeAttributes:
      source: //nas.chezmoi.sh/application-immich
    nodeStageSecretRef:
      name: immich-smb-credentials
      namespace: immich
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: immich-smb-credentials
  namespace: immich
spec:
  data:
    - remoteRef:
        key: lungmen.akn/immich/storage/smb
        property: username
      secretKey: username
    - remoteRef:
        key: lungmen.akn/immich/storage/smb
        property: password
      secretKey: password
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault.chezmoi.sh
  target:
    name: immich-smb-credentials
