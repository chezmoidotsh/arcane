---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: silverbullet-smb-storage
  namespace: silverbullet
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: smb
  volumeName: silverbullet-smb-storage
---
apiVersion: v1
kind: PersistentVolume
metadata:
  annotations:
    pv.kubernetes.io/provisioned-by: smb.csi.k8s.io
  name: silverbullet-smb-storage
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: smb
  mountOptions:
    - dir_mode=0777
    - file_mode=0777
    - uid=25705
    - gid=25705
    - noperm
    - mfsymlinks
    - cache=strict
    - noserverino # required to prevent data corruption
  csi:
    driver: smb.csi.k8s.io
    # volumeHandle format: {smb-server-address}#{sub-dir-name}#{share-name}
    # make sure this value is unique for every share in the cluster
    volumeHandle: smb-server.default.svc.cluster.local/silverbullet##
    volumeAttributes:
      source: //nas.chezmoi.sh/application-silverbullet
    nodeStageSecretRef:
      name: silverbullet-smb-credentials
      namespace: silverbullet
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: silverbullet-smb-credentials
  namespace: silverbullet
spec:
  data:
    - remoteRef:
        key: lungmen.akn/silverbullet/storage/smb
        property: username
      secretKey: username
    - remoteRef:
        key: lungmen.akn/silverbullet/storage/smb
        property: password
      secretKey: password
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault.chezmoi.sh
  target:
    name: silverbullet-smb-credentials
