---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  labels:
    app.kubernetes.io/component: database
    app.kubernetes.io/instance: paperless-ngx-database
  name: paperless-ngx-database
spec:
  bootstrap:
    recovery:
      source: 2025-09-09T16:15:39.825Z
  description: PostgreSQL database dedicated to paperless-ngx
  enablePDB: false
  imageCatalogRef:
    apiGroup: postgresql.cnpg.io
    kind: ClusterImageCatalog
    name: default
    major: 17
  instances: 1
  managed:
    roles:
      - name: paperless
        connectionLimit: -1
        comment: PostgreSQL role for paperless-ngx
        ensure: present
        inherit: true
        login: true
        passwordSecret:
          name: paperless-ngx-database-paperless
  plugins:
    - enabled: true
      isWALArchiver: true
      name: barman-cloud.cloudnative-pg.io
      parameters:
        barmanObjectName: selfhosted
        serverName: 01KFDRRBWFFDV5S5K8HHRMBK0N # 2026-01-20T13:15:56.175Z
  postgresql:
    parameters:
      # Limit WAL retention to avoid DB crash due to storage bloat
      max_slot_wal_keep_size: 900MB
      max_wal_size: 100MB
  storage:
    size: 5Gi
  walStorage:
    size: 5Gi

  externalClusters:
    - name: 2025-09-09T16:15:39.825Z
      plugin:
        enabled: true
        isWALArchiver: false
        name: barman-cloud.cloudnative-pg.io
        parameters:
          barmanObjectName: selfhosted
          serverName: 01K4QM5VSHX9YAAQ46W814EVVM
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: paperless-ngx-database-paperless
spec:
  dataFrom:
    - extract:
        key: lungmen.akn/paperless-ngx/database/postgresql
  refreshInterval: 5m
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault.chezmoi.sh
  target:
    template:
      data:
        username: paperless
        password: "{{ .password }}"
