apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  annotations:
    policy.networking.k8s.io/description: |
      Allows Jellyseerr to access the mutualized PostgreSQL cluster (postgres-apps)
      in the databases namespace.
  name: allow-apps-database-from-jellyseerr
  namespace: databases
spec:
  endpointSelector:
    matchLabels:
      cnpg.io/cluster: postgres-apps
  ingress:
    - fromEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: jellyseerr
            app.kubernetes.io/name: jellyseerr
      toPorts:
        - ports:
            - port: "5432"
              protocol: TCP
