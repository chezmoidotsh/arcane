apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  annotations:
    policy.networking.k8s.io/description: |
      Allows Immich to access the secured PostgreSQL cluster (postgres-apps-secured)
      in the databases namespace.
  name: allow-apps-secured-database-from-immich
  namespace: databases
spec:
  endpointSelector:
    matchLabels:
      cnpg.io/cluster: postgres-apps-secured
  ingress:
    - fromEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: immich
            app.kubernetes.io/name: immich
      toPorts:
        - ports:
            - port: "5432"
              protocol: TCP
