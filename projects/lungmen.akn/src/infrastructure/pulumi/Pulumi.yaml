name: lungmen-akn-infra
runtime: nodejs
description: >-
  Cloud + Vault/OpenBao infrastructure for lungmen.akn: remote cluster Vault
  auth backend, Cloudflare cert-manager token, Tailscale OAuth client.

backend:
  # Garage S3 backend. See docs/procedures/infrastructure/
  # INF-20260705-00.pulumi-state-and-import.md.
  url: "s3://pulumi-states?endpoint=s3.chezmoi.sh&region=fr-par-1"
