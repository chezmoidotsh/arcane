---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  annotations:
    policy.networking.k8s.io/description: |
      Allows Spoolman to access the mutualized PostgreSQL database.
  labels:
    app.kubernetes.io/instance: spoolman
    app.kubernetes.io/name: spoolman
  name: allow-spoolman-to-databases
  namespace: spoolman
spec:
  egress:
  - toEndpoints:
    - matchLabels:
        cnpg.io/cluster: apps-20260329
        io.kubernetes.pod.namespace: databases
    toPorts:
    - ports:
      - port: "5432"
        protocol: TCP
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: spoolman
